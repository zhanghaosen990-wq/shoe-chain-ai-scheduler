# 鞋链智排原型使用说明

## 先说明当前版本能做什么

当前原型是一个可操作的“智能调度流程演示”：它能录入订单、查询模拟工厂数据、按照业务规则生成工厂分配方案、提示风险，并创建模拟的人工审批/询单动作。

它**还不是最终参赛版**，因为此时尚未接入真实大模型、真实企业数据、微信或 ERP 系统。它的作用是先把产品业务闭环做对、做稳，再升级为真正的 AI Agent。

## 第一次启动：最简单的方法

1. 在 Finder 中打开项目文件夹：`/Users/a1/Desktop/永嘉农商杯`。
2. 双击 `启动原型.command`。如 macOS 弹出安全提醒，请右键该文件，选择“打开”，再确认一次。
3. 会出现一个终端窗口，看到“鞋链智排原型运行于 http://localhost:4173”即代表启动成功。
4. 打开浏览器，在地址栏输入 `http://localhost:4173`，按回车。
5. **不要关闭终端窗口**。关闭它，网页就会无法连接；这是本地原型正常的运行方式。

如果双击方式不方便，也可以在 Codex 的终端中运行：

```bash
/Users/a1/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node app/server.js
```

## 默认演示：建议你第一次这样操作

页面打开后，左侧已填好一笔默认紧急订单：800 双商务男鞋、10 天交付、允许拆单。

1. 不修改任何信息，点击绿色按钮“启动智能调度”。
2. 观看中间“Agent 执行过程”。它会依次执行五步，并显示每一步调用的业务工具和结果。
3. 稍等几秒后，页面会显示“拆单协同生产方案”。
4. 查看推荐结果：
   - 瓯越精工鞋业（模拟）：500 双；
   - 楠江鞋业制造（模拟）：300 双；
   - 系统会提示楠江鞋业的准时率风险，以及应该采取的跟进动作。
5. 点击“生成询单消息”，演示 Agent 帮跟单员准备沟通内容；点击“提交人工审批”，演示品牌负责人确认的闭环。

## 怎样自己修改订单测试

左侧订单表单可以修改品类、数量、交期、关键工艺和是否允许拆单。

建议尝试以下两种情况：

| 测试目的 | 怎样填写 | 应该看到什么 |
| --- | --- | --- |
| 测试拆单 | 保持默认 800 双、10 天、允许拆单 | 推荐 A 厂 500 双 + B 厂 300 双 |
| 测试无法安排 | 取消“允许拆单生产”，保持 800 双 | 系统提示需要人工介入，因为没有一家低风险工厂可独立完成 |

注意：关键工艺必须使用当前演示数据中已有的工艺名称：`头层牛皮鞋面`、`固特异外观线`、`橡胶大底`。如果输入完全陌生的工艺，系统找不到匹配工厂，这是符合业务逻辑的正常结果。

## 为什么之前可能看起来“用不了”

1. 本地网页必须先启动服务，直接双击 HTML 或没开服务时，数据无法加载。
2. 原型使用的是本机地址 `localhost:4173`，只能在当前电脑访问，不能把这个地址直接发给其他人。
3. 系统不是所有订单都一定会给出推荐；不符合工艺、交期、起订量或拆单规则时，它应提示人工介入。

## 下一阶段要补的内容

1. 接入真实大模型，让跟单员可直接用自然语言输入订单并获得解释。
2. 将当前本地 JSON 模拟库替换为数据库/API，支持 Excel 导入工厂产能。
3. 部署到公网，生成评委可访问的链接，并准备 5 分钟演示视频。
